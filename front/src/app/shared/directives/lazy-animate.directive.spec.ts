import { LazyAnimateDirective } from './lazy-animate.directive';

describe('LazyAnimateDirective', () => {
  it('should create an instance', () => {
    const directive = new LazyAnimateDirective();
    expect(directive).toBeTruthy();
  });
});
